"""
Authentication package.

This package contains the application's authentication routes
and authentication-related logic.

The actual login, logout, signup, session handling, rate limiting,
account lockout, and authorization checks are implemented in
auth_routes.py and the dedicated security modules.
"""
